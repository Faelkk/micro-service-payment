on:
  push:
    branches:
      - master

jobs:
  deploy-product:
    name: Deploy Product Service
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/railwayapp/cli:latest
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - name: Deploy Product Service to Railway
        working-directory: ./Product.Service
        run: railway up --service=product

  deploy-order:
    name: Deploy Order Service
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/railwayapp/cli:latest
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - name: Deploy Order Service to Railway
        working-directory: ./Order.Service
        run: railway up --service=order

  deploy-payment:
    name: Deploy Payment Service
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/railwayapp/cli:latest
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - name: Deploy Payment Service to Railway
        working-directory: ./Payment.Service
        run: railway up --service=payment

  deploy-apigateway:
    name: Deploy API Gateway (Ocelot)
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/railwayapp/cli:latest
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - name: Deploy API Gateway to Railway
        working-directory: ./ApiGateway.Ocelot
        run: railway up --service=api-gateway
